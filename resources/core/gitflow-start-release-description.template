<h1>Start a release branch for ${repositoryName}</h1>
<p><strong>Internal command executed</strong>: jgitflow:jgitflow:release-start $pluginArgs</p>
<p><strong>Description</strong>: Prepares the project for a release. Creates a release branch and updates pom(s) with the release version.</p>
<p><strong>Arguments to the plugin</strong>: ${pluginArgs}</p>
<ul>
<li> Pushes release branch to the remote upstream</li>
<li> Fails when it finds any snapshot dependencies of the start point.</li>
<li> Automatically assigns the parents version to the maven sub modules</li>
</ul>
   